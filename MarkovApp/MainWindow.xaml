<Window x:Class="MarkovApp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:conv="clr-namespace:MarkovApp.Converters"
        Title="Markov App"
        Background="{StaticResource AppBackgroundBrush}"
        WindowState="Maximized">

    <Window.Resources>
        <conv:EdgeToGeometryConverter x:Key="EdgeConverter"/>
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:InverseBooleanConverter x:Key="InverseBooleanConverter"/>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*" MinWidth="500"/>
            <ColumnDefinition Width="1*" MinWidth="320"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                BorderBrush="{StaticResource AccentBorderBrush}"
                BorderThickness="2"
                CornerRadius="8"
                Margin="5">
            <Canvas Background="{StaticResource MatrixBackgroundBrush}"
                    DataContext="{Binding GraphViewModel}">

                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                        <i:InvokeCommandAction Command="{Binding CanvasLeftClickCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewMouseMove">
                        <i:InvokeCommandAction Command="{Binding CanvasMouseMoveCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                    <i:EventTrigger EventName="PreviewMouseRightButtonUp">
                        <i:InvokeCommandAction Command="{Binding CanvasRightReleaseCommand}" PassEventArgsToCommand="True"/>
                    </i:EventTrigger>
                </i:Interaction.Triggers>

                <Line Stroke="#9B59B6"
                      StrokeThickness="2"
                      StrokeDashArray="4 2"
                      X1="{Binding TempLineStart.X}"
                      Y1="{Binding TempLineStart.Y}"
                      X2="{Binding TempLineEnd.X}"
                      Y2="{Binding TempLineEnd.Y}"
                      Visibility="{Binding TempLineVisibility}"/>

                <ItemsControl ItemsSource="{Binding Nodes}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemContainerStyle>
                        <Style>
                            <Setter Property="Canvas.Left" Value="{Binding X}"/>
                            <Setter Property="Canvas.Top" Value="{Binding Y}"/>
                        </Style>
                    </ItemsControl.ItemContainerStyle>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Width="50" Height="50">
                                <Ellipse Fill="{StaticResource NodeFillBrush}"
                                         Stroke="{StaticResource EdgeBorderBrush}"
                                         StrokeThickness="2"/>
                                <TextBlock Text="{Binding Id}"
                                           Foreground="{StaticResource NodeNumberBrush}"
                                           FontWeight="Bold"
                                           FontSize="20"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Top"
                                           Margin="0,-30,0,0"/>
                                <i:Interaction.Triggers>
                                    <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                        <i:InvokeCommandAction
                                            Command="{Binding DataContext.NodeLeftClickCommand, RelativeSource={RelativeSource AncestorType=Canvas}}"
                                            CommandParameter="{Binding}"/>
                                    </i:EventTrigger>
                                    <i:EventTrigger EventName="PreviewMouseRightButtonDown">
                                        <i:InvokeCommandAction
                                            Command="{Binding DataContext.NodeRightClickCommand, RelativeSource={RelativeSource AncestorType=Canvas}}"
                                            CommandParameter="{Binding}"/>
                                    </i:EventTrigger>
                                </i:Interaction.Triggers>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <ItemsControl ItemsSource="{Binding Edges}">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <Canvas/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>

                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <Path Data="{Binding Converter={StaticResource EdgeConverter}}"
                                      Stroke="{StaticResource EdgeBorderBrush}"
                                      StrokeThickness="10"/>
                                <Path Data="{Binding Converter={StaticResource EdgeConverter}}"
                                      Stroke="{StaticResource EdgeBrush}"
                                      StrokeThickness="4"
                                      Cursor="Hand">
                                    <i:Interaction.Triggers>
                                        <i:EventTrigger EventName="PreviewMouseLeftButtonDown">
                                            <i:InvokeCommandAction
                                                Command="{Binding DataContext.EdgeLeftClickCommand, RelativeSource={RelativeSource AncestorType=Canvas}}"
                                                CommandParameter="{Binding}"/>
                                        </i:EventTrigger>
                                    </i:Interaction.Triggers>
                                </Path>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

            </Canvas>
        </Border>

        <Grid Grid.Column="1"
              Margin="10"
              Background="{StaticResource PanelBackgroundBrush}">

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Margin="10">
                <Button Content="Regular MC"
                        Margin="0,5"
                        Command="{Binding CalculateRegularCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
                <Button Content="Absorbing MC"
                        Margin="0,5"
                        Command="{Binding CalculateAbsorbingCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
                <Button Content="Manual Matrix Input"
                        Margin="0,5"
                        Command="{Binding OpenManualMatrixInputCommand}"/>
                <Button Content="Save State"
                        Margin="0,5"
                        Command="{Binding SaveFullStateCommand}"/>
                <Button Content="Load State"
                        Margin="0,5"
                        Command="{Binding LoadFullStateCommand}"/>
                <Button Content="Clear State"
                        Margin="0,10,0,0"
                        Command="{Binding ClearGraphCommand}"
                        IsEnabled="{Binding IsBusy, Converter={StaticResource InverseBooleanConverter}}"/>
            </StackPanel>

            <Border Grid.Row="1"
                    Margin="10"
                    Padding="5"
                    BorderBrush="{StaticResource AccentBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Background="{StaticResource ControlBackgroundBrush}">
                <DockPanel>
                    <TextBlock Text="Transition Matrix"
                               DockPanel.Dock="Top"
                               Margin="0,0,0,5"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto"
                                  MaxHeight="350">
                        <ItemsControl ItemsSource="{Binding GraphViewModel.TransitionMatrix}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <UniformGrid Columns="{Binding GraphViewModel.Nodes.Count}"
                                                 HorizontalAlignment="Center"
                                                 VerticalAlignment="Top"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBox Width="50"
                                             Height="50"
                                             Text="{Binding Value, UpdateSourceTrigger=PropertyChanged}"
                                             HorizontalContentAlignment="Center"
                                             VerticalContentAlignment="Center"
                                             Background="#222222"
                                             Foreground="LightGreen">
                                        <TextBox.ToolTip>
                                            <ToolTip Background="{StaticResource ControlBackgroundBrush}" 
                                                     Foreground="LightGreen"
                                                     Padding="4">
                                                <TextBlock>
                                                    <Run Text="Cell ("/>
                                                    <Run Text="{Binding Row , Mode = OneWay}"/>
                                                    <Run Text=", "/>
                                                    <Run Text="{Binding Column, Mode = OneWay}"/>
                                                    <Run Text=")"/>
                                                </TextBlock>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </TextBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <Border Grid.Row="2"
                    Margin="10"
                    Padding="5"
                    BorderBrush="{StaticResource AccentBorderBrush}"
                    BorderThickness="1"
                    CornerRadius="6"
                    Background="{StaticResource ControlBackgroundBrush}">
                <StackPanel>
                    <TextBlock Text="Initial State Vector"
                               Margin="0,0,0,5"
                               FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                    <ScrollViewer HorizontalScrollBarVisibility="Auto"
                                  VerticalScrollBarVisibility="Disabled"
                                  MaxHeight="100">
                        <ItemsControl ItemsSource="{Binding GraphViewModel.InitialStateVector}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBox Width="70"
                                             Margin="2"
                                             Background="#222222"
                                             Foreground="LightGreen"
                                             HorizontalContentAlignment="Center"
                                             Text="{Binding Text, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                                        <TextBox.ToolTip>
                                            <ToolTip Background="{StaticResource ControlBackgroundBrush}"
                                                     Foreground="LightGreen"
                                                     Padding="4">
                                                <TextBlock>
                                                    <Run Text="State : "/>
                                                    <Run Text="{Binding Index ,Mode=OneWay}"/>
                                                </TextBlock>
                                            </ToolTip>
                                        </TextBox.ToolTip>
                                    </TextBox>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </Border>

        </Grid>

        <Grid Background="#80000000"
              Visibility="{Binding IsBusy, Converter={StaticResource BoolToVis}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20"/>
                <TextBlock Text="Loading..."
                           Foreground="LightGreen"
                           FontWeight="Bold"
                           FontSize="16"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>